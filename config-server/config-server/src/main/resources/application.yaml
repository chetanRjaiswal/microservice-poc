server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER
  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chetanRjaiswal/microservice-tutorial-config
          clone-on-start: true